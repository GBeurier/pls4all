# Catalog tooling

**Status**: skeleton — empty, awaiting content.

**Will hold**: validate_catalog.py (gate), build_closure.py (dependency walker), render_subset.py (vendored-source emitter for the slim packages)

**Note**: Lands in M2 (this phase).

Landed in M2 (target tree skeleton). See `MERGE_ROADMAP.md` §2 for the full tree.
