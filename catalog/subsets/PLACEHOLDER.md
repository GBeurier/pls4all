# Per-subset YAML descriptors

**Status**: skeleton — empty, awaiting content.

**Will hold**: nirs4all_methods.yaml (full umbrella, active), pls4all.yaml (PLS subset, active), aompls.yaml + nirs4all_preprocessing.yaml + nirs4all_augmentation.yaml + nirs4all_selection.yaml + n4m_transfer.yaml (planned stubs)

**Note**: Lands in M2 (this phase).

Landed in M2 (target tree skeleton). See `MERGE_ROADMAP.md` §2 for the full tree.
