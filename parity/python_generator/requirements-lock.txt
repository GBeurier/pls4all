# Phase 0 lockfile. Re-pin with `pip-compile pyproject.toml --output-file=requirements-lock.txt`
# whenever the pyproject pins change.
#
# nirs4all is intentionally tracked here because at least one Phase 0 fixture
# is regenerated from its SIMPLS implementation in subsequent phases.
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.11.4
joblib==1.4.0
threadpoolctl==3.5.0
nirs4all==0.8.5
