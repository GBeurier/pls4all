# Parity-generator lockfile. Re-pin with `pip-compile pyproject.toml --output-file=requirements-lock.txt`
# whenever the pyproject pins change.
#
# nirs4all is intentionally tracked here for upcoming cross-implementation
# fixtures; current SIMPLS fixtures use a deterministic NumPy port.
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.11.4
joblib==1.4.0
threadpoolctl==3.5.0
nirs4all==0.8.5
