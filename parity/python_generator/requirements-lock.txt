# Pinned lock file for the chemometrics4all parity fixture generator.
#
# Mirrors `pyproject.toml`'s top-level pins and adds the transitive deps
# that scikit-learn / scipy actually drag in at install time. Used by the
# parity-gate CI workflow (G2 from Codex post-batch review) to guarantee
# that fixtures regenerate byte-for-byte across machines.
#
# Generated to match the `pyproject.toml` pins (numpy==1.26.4 et al.). Refresh
# via `pip-compile parity/python_generator/pyproject.toml -o requirements-lock.txt`
# whenever the pyproject pins are bumped.
#
# Install via:
#   pip install -r parity/python_generator/requirements-lock.txt

# Top-level pins (must mirror pyproject.toml)
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.4.2
PyWavelets==1.6.0
pybaselines==1.1.4

# Transitive deps locked by scikit-learn 1.4.2
joblib==1.4.2
threadpoolctl==3.5.0
