# Public n4m_* C ABI headers

**Status**: skeleton — empty, awaiting content.

**Will hold**: n4m.h (umbrella), n4m_version.h, n4m_export.h.in, plus per-category headers: preprocessing.h, augmentation.h, splitters.h, filters.h, utilities.h, models.h, selection.h, diagnostics.h, aom_pop.h, transfer.h, context.h

**Note**: Donor donates the existing n4m.h umbrella; M5/M6 split it into per-category headers as the ABI rename lands.

Landed in M2 (target tree skeleton). See `MERGE_ROADMAP.md` §2 for the full tree.
