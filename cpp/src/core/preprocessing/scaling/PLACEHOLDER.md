# Centering / scaling / log

**Status**: skeleton — empty, awaiting content.

**Will hold**: baseline.c, log_transform.c, normalize.c, simple_scale.c, plus autoscale/pareto from pls4all

**Note**: Donor primarily, pls4all autoscale/pareto in M3/M4.

Landed in M2 (target tree skeleton). See `MERGE_ROADMAP.md` §2 for the full tree.
