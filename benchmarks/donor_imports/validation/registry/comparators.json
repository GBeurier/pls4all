{
  "comparators": [
    {
      "contract_note": "Default C/Python/reference parity comparator.",
      "default_atol": 1e-08,
      "default_rtol": 1e-05,
      "description": "Flattened-output shape check followed by numpy.allclose with equal_nan=True.",
      "id": "default_allclose",
      "metric": "allclose(max_abs_diff, rtol, atol)",
      "name": "default allclose gate"
    },
    {
      "contract_note": "Used by iasls only.",
      "default_atol": 5e-06,
      "default_rtol": 1e-05,
      "description": "Same flattened allclose gate as default_allclose, with a higher absolute tolerance for iterative IAsLS baselines.",
      "id": "iasls_allclose",
      "metric": "allclose(max_abs_diff, rtol, atol)",
      "name": "IAsLS relaxed absolute-tolerance gate"
    },
    {
      "contract_note": "Used by component projection methods.",
      "default_atol": 1e-08,
      "default_rtol": 1e-05,
      "description": "Column signs are aligned before allclose, matching PCA/SVD component sign indeterminacy.",
      "id": "sign_invariant_columns",
      "metric": "sign-aligned allclose",
      "name": "sign-invariant component gate"
    },
    {
      "contract_note": "Used for prospectr Savitzky-Golay parity.",
      "default_atol": 1e-08,
      "default_rtol": 1e-05,
      "description": "Compares the shared interior window when a reference drops boundary columns.",
      "id": "savgol_valid_window",
      "metric": "valid-window allclose",
      "name": "Savitzky-Golay valid-window gate"
    }
  ],
  "schema_version": 1
}
