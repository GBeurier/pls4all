## R CMD check results

Local Linux check:

* 0 errors
* 0 warnings
* 0 notes

## Notes for CRAN

`libn4m` is vendored under `src/libn4m` and compiled into the package shared
object. No external nirs4all-methods system library is required at install or
runtime.
