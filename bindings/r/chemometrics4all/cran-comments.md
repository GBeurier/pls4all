## R CMD check results

Local Linux check:

* 0 errors
* 0 warnings
* 0 notes

## Notes for CRAN

`libc4a` is vendored under `src/libc4a` and compiled into the package shared
object. No external chemometrics4all system library is required at install or
runtime.
