/* SPDX-License-Identifier: CECILL-2.1 */
/* pls4all visibility macros — STATIC vendored copy for the R package.
 * The R build links the C++ sources into pls4all.so (the package shared
 * object that R itself loads), so all n4m_* functions are linked
 * directly — no DLL boundary, no dllexport/dllimport. */
#ifndef PLS4ALL_P4A_EXPORT_H
#define PLS4ALL_P4A_EXPORT_H

#define N4M_API
#define N4M_LOCAL

#endif /* PLS4ALL_P4A_EXPORT_H */
